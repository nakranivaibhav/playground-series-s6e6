"""Reusable, stateless, competition-agnostic scripts for kaggleforge.

Importable as a package (e.g. `from tools.validate_submission import validate`)
when the repo root is on sys.path — a node's solution.py walks up to find it.
"""
